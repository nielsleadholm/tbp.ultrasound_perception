# @package experiment.config.monty_config.learning_modules

defaults:
  - /monty/learning_module/hypotheses_updater/burst_sampling_10nn@learning_module_0

learning_module_0:
  _target_: tbp.monty.frameworks.models.evidence_matching.learning_module.EvidenceGraphLM
  tolerances:
    patch:
      pose_vectors: ${np.array:[0.87266463, 0.87266463, 0.87266463]}
  max_match_distance: 0.01
  x_percent_threshold: 20
  evidence_threshold_config: 80%
  max_graph_size: 0.3
  num_model_voxels_per_dim: 100
  use_multithreading: false
