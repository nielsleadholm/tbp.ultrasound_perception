# @package experiment.config.monty_config

monty_class: ${monty.class:custom_classes.monty_class.MontyForEvidenceGraphMatchingWithGoalStateServer}
monty_args:
  num_exploratory_steps: 200
  min_eval_steps: 20
  min_train_steps: 3
  max_total_steps: 2500
